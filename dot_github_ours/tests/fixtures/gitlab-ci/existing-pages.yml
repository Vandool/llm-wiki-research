stages:
  - build
  - deploy

build:
  stage: build
  script:
    - echo build

pages:
  stage: deploy
  script:
    - mkdir -p public && echo hi > public/index.html
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
