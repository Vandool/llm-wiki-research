# wiki/catalog.yaml — service descriptor (hub-compatible; read by facts.py --seed-catalog and lint L12)
schema_version: 1
name: "{{repo_slug}}"
kind: service
owner: "@{{team}}"
lifecycle: unknown
provides: []
consumes: []
depends_on: []
stacks: []
links:
  repo: "{{gitlab_url}}"
  pages: ""
